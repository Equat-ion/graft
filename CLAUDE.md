# CLAUDE.md

Before touching code, read `docs/` first to understand current system design, workflows, and constraints.

Keep `docs/` up to date whenever code changes alter behavior, architecture, APIs, tools, or testing steps.

Treat `docs/` as source of truth for repo-level context.
