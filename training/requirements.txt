# Notebook dependencies. Install with: pip install -r requirements.txt
# Note: torch + vllm + bitsandbytes are platform-specific; install separately if needed.

# Core
jupyterlab>=4.3.0
ipywidgets>=8.1.5
matplotlib>=3.9.0
pandas>=2.2.0
numpy>=2.0.0
tqdm>=4.66.0

# Git mining
GitPython>=3.1.43

# HTTP
httpx>=0.27.0
packaging>=24.1

# HuggingFace + training
transformers>=4.46.0
datasets>=3.1.0
accelerate>=1.1.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0; platform_system != "Windows"

# Heavy / install separately if you don't already have them:
# torch>=2.5.0
# vllm>=0.6.4
